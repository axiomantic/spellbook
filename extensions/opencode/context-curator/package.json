{
  "name": "spellbook-context-curator",
  "version": "0.1.0",
  "type": "module",
  "description": "OpenCode plugin for intelligent context pruning with spellbook MCP integration",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "clean": "rm -rf dist",
    "build": "npm run clean && tsc",
    "dev": "opencode plugin dev",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/*.test.ts"
  },
  "keywords": [
    "opencode",
    "opencode-plugin",
    "spellbook",
    "context-pruning"
  ],
  "author": "Spellbook",
  "license": "MIT",
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "dependencies": {
    "@anthropic-ai/tokenizer": "^0.0.4",
    "@opencode-ai/sdk": "^1.2.14",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.1.43",
    "@types/node": "^20.0.0",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "files": [
    "dist/",
    "README.md"
  ]
}
