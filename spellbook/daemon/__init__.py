"""Daemon management for the spellbook MCP server.

This package provides start/stop/restart/status/logs operations for running
the MCP server as a persistent system service (launchd on macOS, systemd on
Linux, Task Scheduler on Windows).
"""
