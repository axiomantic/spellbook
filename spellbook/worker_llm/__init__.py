"""Worker-LLM integration (optional OpenAI-compatible client).

All features default OFF. Zero behavior change for users who do not configure
a worker endpoint. See `spellbook/worker_llm/config.py::feature_enabled`.
"""
